{
  "build": {
    "commands": [
      "pip install --upgrade pip",
      "pip install -r requirements.txt"
    ]
  },
  "run": {
    "command": "python start.py"
  },
  "environment": {
    "PORT": "8080"
  }
}